# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Fabric Properties
minecraft_version=1.19.2
loader_version=0.16.14
loom_version=1.9.2

# Mod Properties
mod_version=1.0.3
maven_group=com.hailwall
# MC range this single jar supports (pre-1.20.2 login-phase era). Keep in sync with
# fabric.mod.json "depends.minecraft".
mc_range=1.19-1.19.2

# Dependencies
fabric_api_version=0.77.0+1.19.2
